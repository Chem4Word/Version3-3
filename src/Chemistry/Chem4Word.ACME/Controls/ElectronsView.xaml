<UserControl
    x:Class="Chem4Word.ACME.Controls.ElectronsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enums="clr-namespace:Chem4Word.Model2.Enums;assembly=Chem4Word.Model2"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="154"
    Height="154"
    Loaded="OnLoaded_ElectronsView"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ObjectDataProvider
            x:Key="ElectronTypes"
            MethodName="GetValues"
            ObjectType="{x:Type enums:ElectronType}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:ElectronType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid d:IsLocked="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListView
            x:Name="ElectronsListView"
            Grid.Row="0"
            Margin="2"
            d:IsLocked="True"
            ItemsSource="{Binding ElementName=ThisWindow, Path=Electrons}"
            SelectionChanged="OnSelectionChanged_ElectronsListView"
            SelectionMode="Single">
            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Type">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Width="85">
                                    <ComboBox
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch"
                                        ItemsSource="{Binding Source={StaticResource ElectronTypes}}"
                                        SelectedItem="{Binding TypeOfElectron, Mode=TwoWay}"
                                        SelectionChanged="OnSelectionChanged_Selector" />
                                </Grid>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="1" d:IsLocked="True">
            <Grid d:IsLocked="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button
                    x:Name="Add"
                    Grid.Column="0"
                    Width="Auto"
                    Margin="3"
                    d:IsLocked="True"
                    Click="OnClick_Add">
                    Add
                </Button>
                <Button
                    x:Name="Remove"
                    Grid.Column="1"
                    Width="Auto"
                    Margin="3"
                    d:IsLocked="True"
                    Click="OnClick_Delete">
                    Remove
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
